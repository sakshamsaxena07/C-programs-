#include <stdio.h>
#define PI 3.1416

float areaOfCircle(float r) {
    return PI * r * r;
}

int main() {
    float radius;
    printf("Enter radius: ");
    scanf("%f", &radius);

    printf("Area of Circle = %.2f\n", areaOfCircle(radius));
    return 0;
}
